{
  "name": "constructor-de-apps-de-castelog",
  "version": "1.0.0",
  "description": "Construye apps basadas en la web con Calo.",
  "main": "src/index.1.html",
  "scripts": {
    "build:desktop": "npx pkg bin/client.js -o client",
    "serve": "npx http-server -c-1 src",
    "start": "npx concurrently './node_modules/.bin/shellver-server --password password --port 9047 --app-directory ./src --app-url-path /index.1.html'",
    "test": "npx http-server -c-1 src -o /index.1.html"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "shellver": "^1.0.0"
  }
}
